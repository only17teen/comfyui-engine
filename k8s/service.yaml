apiVersion: v1
kind: Service
metadata:
  name: comfyui-engine
  labels: {app: comfyui-engine}
spec:
  type: ClusterIP
  selector: {app: comfyui-engine}
  ports: [{name: http, port: 80, targetPort: 8080}]
